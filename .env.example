# Anthropic (optional — only needed if not logged in via `claude login`)
# ANTHROPIC_API_KEY=sk-ant-...

# Kalshi credentials
KALSHI_API_KEY_ID=your-api-key-id
# Place your PEM file at ~/.kalshi/private_key.pem (mounted read-only into container)

# Polymarket credentials
POLYMARKET_KEY_ID=your-key-id
POLYMARKET_SECRET_KEY=your-secret-key
