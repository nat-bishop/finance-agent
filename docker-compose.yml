services:
  agent:
    build: .
    env_file: .env
    environment:
      - ANTHROPIC_API_KEY
      - POLYMARKET_KEY_ID
      - POLYMARKET_SECRET_KEY
    volumes:
      - ./workspace:/workspace
      - ~/.kalshi/private_key.pem:/workspace/keys/private_key.pem:ro
    stdin_open: true
    tty: true
